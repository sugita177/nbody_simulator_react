# ----------------------------------------------------
# 1. ビルドステージ
# ----------------------------------------------------
# Node.jsの公式イメージをベースに使用
FROM node:22-alpine AS build

# 作業ディレクトリを設定
WORKDIR /app

# パッケージ管理ファイルをコピーし、依存関係をインストール
COPY package*.json ./
RUN npm install

# プロジェクトのソースコードをコピー
COPY . .

# ----------------------------------------------------
# 2. 開発用ランタイムステージ
# ----------------------------------------------------
# ビルドステージのイメージを使用
FROM build AS development

# 開発サーバーを実行 (Viteのデフォルトポートは5173)
# 'npm run dev'を実行するためのコマンドをENTRYPOINTに設定
# ENTRYPOINT ["npm", "run", "dev"]
# コンテナ起動時に'npm run dev'を実行
CMD ["npm", "run", "dev"]

# コンテナがリッスンするポートを指定
EXPOSE 5173